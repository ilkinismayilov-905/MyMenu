version: '3.8'

services:
  backend:
    image: ghcr.io/${GITHUB_REPO_OWNER}/mymenu-backend:${TAG}
    container_name: ilkin-mymenu-backend-dev
    ports:
      - "8083:8081"  # Expose port 8080 of backend to port 8098 of the host
    environment:
      - SPRING_PROFILES_ACTIVE=dev  # Set profile to dev
    networks:
      - mymenu-dev-network

networks:
  mymenu-dev-network:
    driver: bridge
